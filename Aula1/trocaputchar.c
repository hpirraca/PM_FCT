#include <stdio.h>

main()
	{

 	char caracter1=' ';
 	char caracter2=' ';
 	char caracter3=' ';
 
 	printf("Introduza 3 caracteres: ");
 	putchar (caracter1);
 	putchar (caracter2);
 	putchar (caracter3);
 
 	printf("\n Invertendo a ordem dos caracteres obtemos: ");
 	printf(" '%c' '%c' '%c'\n", caracter3, caracter2, caracter1);
 	}